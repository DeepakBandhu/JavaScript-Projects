function myFunction() { //defining the function
    var sentence = "I am learning"; // the senctence
    sentence += " a lot from this book!";   // that will show up 
    document.getElementById("Concatenate").innerHTML=sentence; // defining the id
}